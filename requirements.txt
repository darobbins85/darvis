# Core dependencies
pyaudio>=0.2.13
pyttsx3>=2.90
SpeechRecognition>=3.10.0
Pillow>=10.0.0

# GUI and system tray
pystray>=0.19.0

# Web interface and synchronization
flask>=2.0.0
flask-socketio>=5.0.0
python-socketio[client]>=5.0.0

# Optional: GTK support for system tray (Linux)
PyGObject>=3.42.0

# E2E Testing Dependencies
pytest>=7.0.0
psutil>=5.9.0
# pyautogui>=0.9.53  # For GUI automation (optional, uncomment if available)
# opencv-python>=4.8.0  # For image recognition in GUI tests (optional)
# pytesseract>=0.3.10  # For OCR in GUI verification (optional)